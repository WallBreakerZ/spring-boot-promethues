spring:
  application:
    name: springbootPrometheusGrafana

management:
  endpoints:
    web:
      exposure:
        include:  '*'
      base-path: "/actuator"
  metrics:
    tags:
      application: ${spring.application.name}
